anatoly_protestes = {
 	anatoly_protestes_z = {
		icon = generic_army_support
			available = {

				CRI = { 
					NOT = {
					has_idea = CRI_anatoly_protestes
					}
				}
			hidden_trigger = { always = no }
			}
			
			fire_only_once = yes
			
			allowed = { tag = HAI }
			
			days_mission_timeout = 62
			is_good = no

			timeout_effect = {
				CRI = { country_event = crimea.1 }
			}
			
			activation = {
			}
			
			visible = {
			}
 	}
}